# v0.2.0 clean-install dry run — 2026-09-25

Fresh clone of this branch, empty Docker volume, following the README quickstart exactly.
Ports remapped to 8010/8011 only to avoid a clash on the test machine.

| Step | Command | Result |
|------|---------|--------|
| Start | `docker compose up -d` | healthy in ~60 s (includes image build) |
| Setup | `docker compose exec api monkey-mind setup` → `/notes` | `3 page(s) ingested (6 chunks)`, test retrieval OK, "ready" |
| CLI query | `monkey-mind query --user outsider "Can I fit the release and my training this week?"` | Cross-domain answer citing `/notes/strategy/goals.md`, `/notes/health/training.md`, `/notes/projects/release.md` |
| Re-ingest | add a note, `monkey-mind ingest --connector files --user outsider` | `1 new, 2 updated` — writes to the store |
| REST query | `POST /query` with `X-API-Key` | 200, grounded answer |
| Old README header | `Authorization: Bearer` | 401 (why the docs changed) |
| Eval | `monkey-mind eval --api-key …` | **9/9** |
| MCP | `GET :8001/.well-known/oauth-authorization-server` | 200 |
| Version | `monkey-mind --version` | `monkey-mind 0.2.0` |

Before this branch the same path failed at step 4 of the old README (`user create` has no connector),
`ingest` wrote nothing, `query` was a stub, `eval` never ran, and eval scored 6/9 once forced
(S7 provenance: sources were just `files` with no date).

Not covered here: live OAuth round-trip from claude.ai / ChatGPT against a hosted instance,
Path B (local install), GitHub connector. Needs an outside tester on a Mac for the installer.
