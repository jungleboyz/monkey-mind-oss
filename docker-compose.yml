version: "3.9"

services:

  api:
    build: .
    image: monkeymind/api:latest
    command: uvicorn mm.api.server:app --host 0.0.0.0 --port 8000
    ports:
      - "${API_PORT:-8000}:8000"
    volumes:
      - mm_data:/data
    environment:
      - DATA_ROOT=/data
      - OPENAI_API_KEY
      - ANTHROPIC_API_KEY
      - MM_LLM_PROVIDER=${MM_LLM_PROVIDER:-anthropic}
      - MM_EMBED_PROVIDER=${MM_EMBED_PROVIDER:-openai}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp:
    build: .
    image: monkeymind/api:latest
    command: python -m mm.mcp.server --transport http --port 8001
    ports:
      - "${MCP_PORT:-8001}:8001"
    volumes:
      - mm_data:/data
    environment:
      - DATA_ROOT=/data
      - USER_ID=${MM_USER_ID:-default}
      - OPENAI_API_KEY
      - ANTHROPIC_API_KEY
      - MM_LLM_PROVIDER=${MM_LLM_PROVIDER:-anthropic}
      - MM_EMBED_PROVIDER=${MM_EMBED_PROVIDER:-openai}
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

volumes:
  mm_data:
    driver: local
